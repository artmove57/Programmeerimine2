<Window x:Class="KooliProjekt.WpfClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KooliProjekt.WpfClient"
        xmlns:viewmodels="clr-namespace:KooliProjekt.WpfClient.ViewModels"
        mc:Ignorable="d"
        Title="Teams Management - MVVM Pattern" Height="600" Width="1000">

    <!-- ViewModel seos -->
    <Window.DataContext>
        <viewmodels:MainWindowViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Pealkiri -->
        <TextBlock Grid.Row="0" 
                   Text="Teams Management (MVVM Pattern)" 
                   FontSize="24" FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <!-- Sisu: DataGrid + Nupud -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- DataGrid meeskondadega -->
            <DataGrid Grid.Row="0" 
                      ItemsSource="{Binding Teams}"
                      SelectedItem="{Binding SelectedTeam, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      Margin="0,0,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" 
                                        Binding="{Binding Id}" 
                                        Width="80" 
                                        IsReadOnly="True"/>
                    <DataGridTextColumn Header="Meeskonna Nimi" 
                                        Binding="{Binding Name}" 
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Nupud -->
            <StackPanel Grid.Row="1" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        Margin="0,10,0,0">

                <!-- Värskenda nupp -->
                <Button Content="Värskenda" 
                        Command="{Binding LoadCommand}"
                        Width="120" Height="35" 
                        Margin="5" FontSize="14"/>

                <!-- Uus nupp -->
                <Button Content="Uus" 
                        Command="{Binding NewCommand}"
                        Width="120" Height="35" 
                        Margin="5" FontSize="14"
                        Background="LightBlue"/>

                <!-- Salvesta nupp -->
                <Button Content="Salvesta" 
                        Command="{Binding SaveCommand}"
                        Width="120" Height="35" 
                        Margin="5" FontSize="14"
                        Background="LightGreen"/>

                <!-- Kustuta nupp -->
                <Button Content="Kustuta" 
                        Command="{Binding DeleteCommand}"
                        Width="120" Height="35" 
                        Margin="5" FontSize="14"
                        Background="LightCoral"/>
            </StackPanel>
        </Grid>

        <!-- Olekuriba -->
        <Border Grid.Row="2" 
                Background="LightGray" 
                Padding="10" 
                Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusMessage}" 
                       FontSize="12"/>
        </Border>
    </Grid>
</Window>
